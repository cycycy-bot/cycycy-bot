version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    working_directory: ~/cycycy-bot
    steps:
      - checkout
      - run:
          name: install dependencies
          command: 'npm install'
      - run: npm test